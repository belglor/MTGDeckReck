# Copy to .env (gitignored) and fill in values you want to override.
# Everything here is optional; see src/mtg_rag/ingest/config.py for defaults.

# Scryfall asks API clients to identify themselves with real contact info.
# The default carries none.
SCRYFALL_USER_AGENT=MTGDeckReck/0.1 (you@example.com)
